
export interface FileData {
  isfolder: boolean,
  id: string;
  name: string;
  extension: string;
  lastmodified: string;
  size: string;
}

export interface FilterData {
  name: string,
  filter: string,
}
